# Deferred External Requirements

This register separates locally completable engineering from authenticated provider, production, legal, business, and irreversible actions. A deferred item is not evidence of successful verification and does not satisfy preflight or production readiness.

Current preflight refresh (H-211): `sh scripts/preflight.sh` reports 23 unresolved requirements: unavailable local database/worker/Redis/R2/OTLP probes, Resend, Turnstile, Sentry, GitHub, Fly.io targets, legal/vendor/insurance/DPIA/retention evidence, and signed hosted worker sandbox evidence plus its trusted public key. The production-bound probe additionally requires `WORKER_SANDBOX_EVIDENCE_REQUIRE_BINDING=1` with expected image digest, Fly app, and worker identity. This supersedes older aggregate counts in historical checkpoint prose below.

Latest local evidence (current continuation): all sixteen live-fire proof commands pass against the real local PostgreSQL, Redis, MinIO, Mailpit, and OTLP services; authenticated DeepSeek cache/provenance and Deepgram sample transcription proofs pass using ignored local credentials; the SQL outbox dispatcher has lease-token fencing plus real claim/lease/completion/retry/dead-letter/unsupported-job coverage, validated UUID archive partitioning, and a real archive-partition regression; the privacy intake handler validates authoritative rows, records hashed-requester review evidence, and creates a queued deletion hold without claiming fulfillment; export and narration intake validate authoritative rows, transition to running, and append review-required audit events without fabricating provider effects; bearer tokens now carry random session IDs and the production server wires a hashed Redis revocation deny-list with real persistence and API rejection proofs; upload sessions record initiating users, enforce eight-session/25 GiB user and 50 GiB archive active reservations with transaction-scoped advisory-lock serialization, and append completed storage bytes to the usage ledger; archive outbox capacity checks use the same transaction-scoped serialization and real concurrent PostgreSQL regressions; the API limiter is now an atomic Redis fixed-window limiter with hashed source-IP, principal, and archive keys, fail-closed request handling, and real local Redis integration plus API regression proof; member management, rights subjects, pending-only rights/media intake, private share creation, privacy-write authorization, provider-authoritative billing status, catalog plan validation, one-current-subscription enforcement, durable signature-verified Stripe callback ingestion with exact-payload replay checks, strict HTTPS production CORS allowlist validation, and upload MIME/fixity checks are enforced at the API boundary, including bounded magic-byte validation and a real MinIO mismatch rejection proof; derivative recipes are database-unique and worker retries compare SHA-256 fixity before accepting existing objects; worker derivatives are bounded to 256 MiB per artifact and 512 MiB per job before buffering; the dedicated worker image builds with the required media executables; authenticated local MinIO bucket access, custom-format PostgreSQL backup, disposable restore-check with ten migrations, media subprocess secret isolation, strict bearer parsing, provider endpoint/retry validation, recursive AI-input redaction, exact-result cache isolation, and telemetry/provider-secret redaction pass locally. HARDENING-79 additionally fences privacy/export/narration intake writes to the active lease and locks media quarantine transitions. HARDENING-80 now requires a hosted worker sandbox attestation, HARDENING-170 now requires a trusted Ed25519 public key and verifies the signed attestation payload before acceptance, and HARDENING-173 fails production worker startup closed when observable Linux sandbox controls are absent. HARDENING-176 now requires a distinct `FLY_APP_WORKER_PRODUCTION` target and paired API/worker manual deployment commands. The latest preflight reports 18 unresolved requirements: the Resend credential probe, Turnstile, Sentry, GitHub, Fly.io including the private production worker app, legal/vendor/insurance/DPIA/retention evidence, and the hosted worker sandbox attestation plus trusted public key. It remains fail-closed. The direct host integration wrapper also remains unavailable because the intentionally internal-only Compose network does not publish PostgreSQL, Redis, or MinIO host ports; historical disposable internal-runner evidence remains separately identified below.

HARDENING-120 additionally bounds Stripe webhook payload/header materialization, rejects ambiguous timestamp fields, and validates rotated `v1` signatures; HARDENING-121 rejects bodyless provider responses without a declared length before allocation; HARDENING-122 bounds portable export materialization; HARDENING-123 bounds provenance chain aggregation; HARDENING-124 bounds edition manifest contracts; HARDENING-125 bounds direct Stripe callback persistence payloads. Provider-focused and full local verification passed. These changes alter no external requirement and do not authorize production billing activation.

| ID | Service, credential, approval, or action | Environment variables or artifact | Classification | Required | Minimum permission or scope | Blueprint files | Graph nodes | Features and proofs affected | Local alternative | Work possible without it | Exact operator action | Read-only probe | Validation after supply | Local development blocked | Production release blocked | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| EXT-001 | PostgreSQL | `DATABASE_URL` | LOCAL_REAL_SERVICE | Yes | Dedicated local database owner; production runtime least privilege later | PREFLIGHT.md, ARCHITECTURE.md, TESTING.md | EP-000, EP-003-EP-010 | Persistence, RLS, migrations, integration, backup/restore | PostgreSQL 17 plus pgvector container | Schema, repositories, migrations, and real local tests | Supply a nonproduction Neon URL only for hosted verification | `sh scripts/probes/database_url.sh` | `sh scripts/preflight.sh` | No | Yes | Local real service verified; hosted deferred |
| EXT-024 | Worker PostgreSQL login | `WORKER_DATABASE_URL` | REQUIRED_EXTERNAL_SANDBOX | Yes | `family_historian_worker`; LOGIN, NOINHERIT, no superuser/role/database creation, no RLS bypass | PREFLIGHT.md, DEPLOYMENT.md, SECURITY.md, drizzle/0013_worker_database_role.sql | EP-000, EP-003, EP-007-EP-010 | Queue dispatch and media-worker tenant transactions | Local PostgreSQL role provisioned by migration; local Compose worker URL | Dedicated role migration, startup validation, queue policy, and privilege verifier | Create a production worker login with the exact restricted role attributes and inject its secret into the worker only | `sh scripts/probes/database_url.sh` | `corepack pnpm --filter @family-historian/database migrate && corepack pnpm --filter @family-historian/database verify` | No | Yes | Local role SQL/login probe passed; package migration through host port remains unavailable and production credential is deferred |
| EXT-025 | Worker scratch capacity for maximum accepted media | Hosted worker encrypted scratch volume and `compose.yaml` worker scratch declaration | REQUIRED_EXTERNAL_SANDBOX | Yes | Capacity for at least 25 GiB original plus 512 MiB bounded derivatives and filesystem overhead | ARCHITECTURE.md, SECURITY.md, DEPLOYMENT.md, fly.worker.toml, compose.yaml, packages/media/src/index.ts | EP-003, EP-007, EP-009, EP-010 | Large media scans, retry behavior, hosted media release | Runtime statfs preflight and local bounded-capacity rejection | Scratch-capacity guard, streamed download, bounded derivatives, non-retryable capacity errors, and real small-media fixture | Attach an encrypted hosted worker volume sized for the documented maximum and bind it to the worker scratch path; update the deployment manifest before staging | `df -B1 /tmp` inside the worker plus `sh scripts/probes/worker_sandbox_evidence.sh` | `sh scripts/preflight.sh && sh scripts/production-readiness-check.sh && sh scripts/live-fire.sh` | No | Yes | Deferred; local profile intentionally rejects inputs that exceed its declared scratch capacity, while hosted capacity is not provisioned or attested |
| EXT-002 | Redis/queue service | `REDIS_URL` | LOCAL_REAL_SERVICE | Yes | Isolated local Redis; isolated Upstash database later | PREFLIGHT.md, ARCHITECTURE.md, TESTING.md | EP-000, EP-004, EP-007-EP-010 | Queues, locks, rate limits, exact-result cache | Redis container | Queue and cache behavior against a real local service | Supply an isolated Upstash URL for hosted verification | `sh scripts/probes/redis_url.sh` | `sh scripts/preflight.sh` | No | Yes | Local real service verified; hosted deferred |
| EXT-003 | S3-compatible object storage / Cloudflare R2 | `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET`, `R2_ENDPOINT` | REQUIRED_EXTERNAL_SANDBOX | Yes | Object read/write/delete for one project bucket | PREFLIGHT.md, ARCHITECTURE.md, TESTING.md | EP-000, EP-003-EP-010 | Multipart upload, resumable part listing, immutable originals, streamed fixity, derivatives, exports, deletion | Private MinIO bucket with generated credentials | Full storage adapter, signed multipart API E2E, provider-listed resume metadata, actual-byte SHA-256 verification, authenticated bucket probe, local backup/restore rehearsal | Create a nonproduction R2 bucket and scoped API token | `sh scripts/probes/r2.sh` | `sh scripts/preflight.sh` | No | Yes | Authenticated local MinIO bucket probe and multipart flows passed; hosted R2 remains deferred |
| EXT-004 | DeepSeek inference | `DEEPSEEK_API_KEY` | REQUIRED_EXTERNAL_SANDBOX | Yes for external AI proof; optional per archive | Inference and model-list access only | PREFLIGHT.md, AI_PROCESSING_NOTICE.md, ARCHITECTURE.md | EP-000, EP-004, EP-007-EP-010 | Editorial generation, cache telemetry, provider live-fire | Manual non-AI workflow; deterministic gateway tests | Gateway, redaction, policy, cache, schemas, adapter, failure handling, authenticated cache/provenance live-fire | Obtain production-scoped key and current vendor approval; rotate the development key after this run | `sh scripts/probes/deepseek_api_key.sh` | `sh scripts/preflight.sh` then `sh scripts/live-fire.sh` | No | Yes | Newly supplied key authenticated probe passed; production vendor evidence and rotation remain deferred |
| EXT-005 | Deepgram transcription | `DEEPGRAM_API_KEY` | OPTIONAL_EXTERNAL_SANDBOX | Yes in preflight; local fallback specified | Speech-to-text only | PREFLIGHT.md, ARCHITECTURE.md, SUBPROCESSOR_REGISTER.md | EP-000, EP-004, EP-007-EP-010 | Hosted transcription live-fire | Local Whisper | Consent and transcription workflow plus local processing | Maintain the scoped STT key and obtain vendor approval; run authenticated transcription proof | `sh scripts/probes/deepgram_api_key.sh` | `sh scripts/preflight.sh` then `sh scripts/live-fire.sh` | No | Yes under current gate | Authenticated Deepgram projects probe passed; transcription media proof and vendor approval remain deferred |
| EXT-006 | ElevenLabs narration | `ELEVENLABS_API_KEY` | OPTIONAL_EXTERNAL_SANDBOX | No | Licensed stock TTS and verified self-voice endpoints only | PREFLIGHT.md, VOICE_AND_LIKENESS_POLICY.md | EP-000, EP-004, EP-006-EP-010 | Hosted narration and self-voice proof | Stock/local non-cloned narration workflow | Policy enforcement, adapter, request tests, non-voice exports | Supply a sandbox key only after voice/vendor approval | `sh scripts/probes/elevenlabs_api_key.sh` | `sh scripts/live-fire.sh` | No | No unless enabled | Deferred optional |
| EXT-007 | Stripe test mode | `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID` | REQUIRED_EXTERNAL_SANDBOX | Yes for paid launch | Test-mode billing and webhook access | PREFLIGHT.md, TERMS_OF_SERVICE_DRAFT.md | EP-000, EP-004, EP-007-EP-010 | Checkout, webhook delivery, billing live-fire | Local/test entitlement command, never production | Plan catalog, subscription states, quotas, usage idempotency, durable signed callback ingestion, fail-closed local billing proof, authenticated Checkout Session creation | Create test product/price and restricted test key/webhook secret; deliver signed sandbox webhooks to a reachable endpoint and run reconciliation | `sh scripts/probes/stripe.sh` and `sh scripts/probes/stripe_checkout.sh` | `sh scripts/preflight.sh` then `sh scripts/live-fire.sh` | No | Yes | Balance and authenticated test Checkout Session probes passed; signed sandbox delivery, downstream reconciliation, and production billing approval remain deferred |
| EXT-008 | Transactional email / Resend | `RESEND_API_KEY`, `EMAIL_FROM` | REQUIRED_EXTERNAL_SANDBOX | Yes for production email | Sending-domain access only | PREFLIGHT.md, ARCHITECTURE.md | EP-000, EP-004, EP-007-EP-010 | Invitations and job notices | Mailpit SMTP capture on dedicated loopback ports | Production adapter and real local SMTP assertions | Verify a sending domain and supply scoped API key/from address; run a real recipient delivery proof | `sh scripts/probes/resend.sh` | `sh scripts/preflight.sh` then `sh scripts/live-fire.sh` | No | Yes | Current preflight unresolved; direct probe fails when `RESEND_API_KEY` is unset in the shell; a historical domains probe passed, but no current sender or recipient delivery is claimed |
| EXT-009 | Cloudflare Turnstile | `TURNSTILE_SITE_KEY`, `TURNSTILE_SECRET_KEY` | REQUIRED_EXTERNAL_SANDBOX | Yes for production abuse controls | One nonproduction site/widget | PREFLIGHT.md, SECURITY.md | EP-000, EP-006-EP-010 | Abuse-prevention live proof | Official test keys where permitted | Adapter, server verification, fail-closed and deterministic tests | Create a widget and supply its site/secret keys | `sh scripts/probes/turnstile.sh` | `sh scripts/preflight.sh` | No | Yes | Deferred |
| EXT-010 | Sentry | `SENTRY_DSN`, `SENTRY_AUTH_TOKEN` | OPTIONAL_EXTERNAL_SANDBOX | Required by current preflight | Event ingest and release upload for one project | PREFLIGHT.md, OBSERVABILITY.md | EP-000, EP-008-EP-010 | Hosted redacted error proof | Local structured telemetry sink | Scrubbing, redaction and adapter behavior | Create a project and scoped ingest/release credentials | `sh scripts/probes/sentry.sh` | `sh scripts/preflight.sh` | No | Yes under current gate | Deferred |
| EXT-011 | OpenTelemetry backend | `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_EXPORTER_OTLP_HEADERS` | OPTIONAL_EXTERNAL_SANDBOX | Endpoint required; headers optional | Dedicated nonproduction ingest | PREFLIGHT.md, OBSERVABILITY.md | EP-000, EP-008-EP-010 | Hosted metrics/traces | Local OTel collector | Real local export, content exclusion and alerts | Supply endpoint and least-scope headers | `sh scripts/probes/otel.sh` | `sh scripts/preflight.sh` | No | Yes under current gate | Local collector verified; hosted backend deferred |
| EXT-012 | GitHub repository and registry | `GITHUB_TOKEN`, `GITHUB_REPOSITORY` | REQUIRED_EXTERNAL_SANDBOX | Yes for CI/registry | Repository and packages scopes only | PREFLIGHT.md, DEPLOYMENT.md | EP-000, EP-001, EP-009, EP-010 | Remote CI and image registry proof | Local Git and image builds | Workflows and local verification | Create private repository/registry authorization and supply scoped token | `sh scripts/probes/github.sh` | `sh scripts/preflight.sh` | No | Yes | Deferred; variable mismatch under repair |
| EXT-013 | Fly.io staging/production | `FLY_API_TOKEN`, `FLY_APP_STAGING`, `FLY_APP_PRODUCTION`, `FLY_APP_WORKER_PRODUCTION` | IRREVERSIBLE_EXTERNAL_ACTION | Yes for hosted release | Deploy only to named API and private worker apps | PREFLIGHT.md, DEPLOYMENT.md | EP-000, EP-009, EP-010 | Staging deployment, smoke, paired API/worker production deploy and rollback | Local production-mode container rehearsal | Images, manifests, migrations, smoke and rollback scripts | Create the named API and private worker apps, provide a scoped token, and manually authorize paired API/worker deployment | `sh scripts/probes/fly.sh` | `sh scripts/preflight.sh` then `sh scripts/production-readiness-check.sh` and the paired commands in `DEPLOYMENT.md` | No | Yes | Deferred; no production mutation authorized |
| EXT-014 | Application cryptographic secrets | `SESSION_SECRET`, `FIELD_ENCRYPTION_MASTER_KEY`, `DOWNLOAD_SIGNING_SECRET`, `BACKUP_ENCRYPTION_KEY` | LOCAL_GENERATABLE | Yes | Random local-only values; production backup key must be KMS/secret-manager controlled | PREFLIGHT.md, SECURITY.md | EP-000, EP-001, EP-006-EP-010 | Sessions, envelope encryption, download signing, encrypted backups | OS-generated local values | All local cryptographic flows, encrypted backup/restore rehearsal | Generate new production values in the production secret manager and configure KMS-wrapped backup encryption | Presence in `sh scripts/preflight.sh` | Startup and backup configuration validation | No | Yes until production injection | Generated locally and ignored; encrypted local backup/restore passed; production values and KMS wrapping deferred |
| EXT-015 | Legal launch approval | `LEGAL_APPROVAL_FILE` and counsel evidence | LEGAL_OR_BUSINESS_EVIDENCE | Yes | Signed counsel approval for launch jurisdictions | PREFLIGHT.md and all draft legal policies | EP-000, EP-006, EP-009, EP-010 | Recording, rights, publication and launch | None | All technical controls and review workflows | Obtain signed counsel approval and place artifact outside Git | File presence check in `sh scripts/preflight.sh` | `sh scripts/production-readiness-check.sh` | No | Yes | Deferred |
| EXT-016 | Vendor-risk approval | `VENDOR_RISK_APPROVAL_FILE` | LEGAL_OR_BUSINESS_EVIDENCE | Yes | Current approved data-flow/vendor assessment | PREFLIGHT.md, SUBPROCESSOR_REGISTER.md, DPIA.md | EP-000, EP-004, EP-008-EP-010 | External processors and release | Local/off providers | Adapters, isolation, minimization, local alternatives | Complete and sign provider-by-provider assessment | File presence check in `sh scripts/preflight.sh` | `sh scripts/production-readiness-check.sh` | No | Yes | Deferred |
| EXT-017 | Insurance evidence | `INSURANCE_EVIDENCE_FILE` | LEGAL_OR_BUSINESS_EVIDENCE | Yes | Active cyber, technology E&O and media liability | PREFLIGHT.md, PRODUCTION_READINESS.md | EP-000, EP-010 | Production launch | None | All engineering | Obtain active certificates and place artifact outside Git | File presence check in `sh scripts/preflight.sh` | `sh scripts/production-readiness-check.sh` | No | Yes | Deferred |
| EXT-018 | Approved DPIA | `DPIA_APPROVAL_FILE` | LEGAL_OR_BUSINESS_EVIDENCE | Yes | Signed residual-risk and transfer decision | PREFLIGHT.md, DPIA.md | EP-000, EP-006, EP-010 | Production processing | None | Technical privacy controls | Obtain leadership/counsel approval and place artifact outside Git | File presence check in `sh scripts/preflight.sh` | `sh scripts/production-readiness-check.sh` | No | Yes | Deferred |
| EXT-019 | Approved retention schedule | `RETENTION_APPROVAL_FILE` | LEGAL_OR_BUSINESS_EVIDENCE | Yes | Signed category periods and legal bases | PREFLIGHT.md, DATA_RETENTION_SCHEDULE.md | EP-000, EP-006, EP-008, EP-010 | Deletion, backups and legal retention | Configurable unapproved local defaults only | Retention engine and testable policies | Obtain approval and place artifact outside Git | File presence check in `sh scripts/preflight.sh` | `sh scripts/production-readiness-check.sh` | No | Yes | Deferred |
| EXT-020 | Production data-region and data-broker determinations | `compliance/evidence/data-region-verification.md`, `compliance/evidence/data-broker-determination.md` | LEGAL_OR_BUSINESS_EVIDENCE | Yes | Signed, current determinations | PRODUCTION_READINESS.md, production-readiness script | EP-010 | Production launch | None | Technical region constraints and no-sale controls | Obtain and record signed determinations | `sh scripts/production-readiness-check.sh` | `sh scripts/production-readiness-check.sh` | No | Yes | Deferred |
| EXT-021 | Production deployment and DNS | Fly app state, Cloudflare DNS/WAF, release authorization | IRREVERSIBLE_EXTERNAL_ACTION | Yes | Named production resources and explicit manual approval | BLUEPRINT_INPUT.md, DEPLOYMENT.md | EP-009, EP-010 | Public production release | Local deployment rehearsal | Full deploy/rollback artifacts and local proof | Supply resources and run the documented manual deploy command | Staging smoke command to be materialized in EP-009 | `sh scripts/production-readiness-check.sh` then manual deploy | No | Yes | Deferred |
| EXT-022 | Local media toolchain | `ffmpeg`, `ffprobe`, `exiftool`, `magick`, `clamscan`, `ocrmypdf`, `python` | LOCAL_REAL_SERVICE | Required for media live-fire | Local sandboxed executables only; no provider account | PREFLIGHT.md, ARCHITECTURE.md, SECURITY.md, TESTING.md | EP-003, EP-007-EP-010 | Malware scan, metadata normalization, derivatives, OCR, media fixture proof | Deterministic media command plans, no-shell worker executor, and quarantine state machine | Adapter contracts, path confinement, timeout/output bounds, immutability checks, bounded argv plans, all non-media gates | Use the dedicated `worker-runtime` image and run representative fixtures; host installs remain optional | `sh scripts/media-tools-check.sh` (host) | `docker build --target worker-runtime -t family-historian-worker:local .` then the worker-image tool probe and media fixture/live-fire gate | No | Yes for media release | Worker image verified locally; host probe remains deferred and representative fixture/live-fire remains pending |
| EXT-023 | Hosted worker sandbox attestation and trusted signing key | `WORKER_SANDBOX_EVIDENCE_FILE`, `WORKER_SANDBOX_EVIDENCE_PUBLIC_KEY_FILE` locally; `WORKER_SANDBOX_EVIDENCE`, `WORKER_SANDBOX_EVIDENCE_PUBLIC_KEY` GitHub secrets in release CI | LEGAL_OR_BUSINESS_EVIDENCE | Yes | Current Ed25519-signed staging/production-equivalent evidence for syscall, network-egress, cgroup/PID, read-only-root, and bounded-scratch controls plus the operator-owned public key | SECURITY.md, PREFLIGHT.md, DEPLOYMENT.md, `.github/workflows/release.yml` | EP-000, EP-009, EP-010 | Hosted media/parser isolation and production release | Local Compose sandbox declarations and worker fixture | Local worker image, no-shell executor, resource ceilings, internal-only network proofs, and cryptographic signature verification | Produce a current signed attestation and trusted Ed25519 public key outside Git; set both local paths or configure both release secrets | `sh scripts/probes/worker_sandbox_evidence.sh` | `sh scripts/preflight.sh` then `sh scripts/production-readiness-check.sh` | No | Yes | Deferred; hosted enforcement and operator evidence remain unverified |

Latest hardening evidence: provider redirects are now rejected before dispatch (HARDENING-160), and worker access to the global `auth_sessions` table is revoked and locally verified (HARDENING-161). The current security scan still reports the broader worker runtime-role/tenant-context boundary as an open high finding, in addition to hosted sandbox and scratch-capacity evidence. These are not marked satisfied by local tests.
HARDENING-162 corrected deployment wiring locally: staging now deploys `fly.worker.toml` separately so native media tooling is present in the worker image. Hosted app/resource creation and sandbox enforcement remain external.
HARDENING-163 added the hosted scratch-volume declaration and exact `fly volumes create` command. The volume must be created and inspected in the target Fly app; no external resource was created during this run.
HARDENING-164 now rejects arbitrary sandbox-evidence text and requires structured current JSON with all isolation controls affirmed. HARDENING-170 additionally verifies an Ed25519 signature against an operator-supplied trusted public key; hosted enforcement and the current signed evidence still require operator evidence.

Latest continuation evidence: HARDENING-26 adds `POST /v1/session/logout`, which requires a signed session identifier and the configured Redis revocation store, revokes the current session through its expiry, returns `204`, and is proven by unit coverage; subsequent authenticated requests are rejected by the request hook. Administrative session inventory, rotation, membership revalidation, passkeys, and hosted identity verification remain deferred release requirements.

HARDENING-27 evidence: the opt-in local worker profile and Fly worker manifest now declare read-only filesystems, dropped capabilities, `no-new-privileges`, bounded `/tmp`, 256 PIDs, 2 GiB memory, 2 CPUs, and graceful termination; `docker compose --profile worker config --quiet` and worker-sandbox unit regressions pass. Hosted syscall, network-egress, and cgroup enforcement still require deployment-platform verification.

HARDENING-28 evidence: `tests/security/run.ts` now performs behavioral checks for API security controls, non-root worker/sandbox declarations, valid-versus-mismatched media signatures, and revocable session identifiers. `corepack pnpm security:check`, typecheck, and all 24 unit files/76 tests pass; this local gate does not replace hosted identity, network, syscall, or secret-manager evidence.

HARDENING-29 evidence: the production server wires authoritative membership revalidation into request hooks and archive listing; a revoked membership now returns `AUTH_REQUIRED` before the route executes. Unit coverage is 77 tests, integration 27, and E2E 11; full local gates plus reality, smoke, performance, live-fire, encrypted backup, and restore-check pass. Native login/session inventory, device rotation, passkeys, and hosted identity verification remain deferred.

HARDENING-30 evidence: `DEPLOYMENT.md` now uses the same `ghcr.io/${GITHUB_REPOSITORY}:${RELEASE_TAG}` image identity as the release workflow; the stale `GHCR_OWNER/family-historian` reference is removed. This is documentation/configuration hardening only; hosted registry and Fly probes remain deferred.

HARDENING-31 evidence: the opt-in local worker rehearsal now shares an internal-only Docker network with PostgreSQL, Redis, and object storage, preventing direct internet egress while preserving required local dependencies; `docker compose --profile worker config --quiet`, Prettier, and focused worker-sandbox tests pass. Hosted network-policy and egress enforcement remain deployment-platform gates.

HARDENING-33 evidence: signed Stripe callback ingestion now verifies that the authoritative `family_archives` row belongs to the metadata organization inside the tenant-scoped transaction; a real integration regression rejects the mismatched pair before persistence. Hosted Stripe delivery and downstream reconciliation remain deferred.

HARDENING-34 evidence: API archive service entry points now validate that the requested archive belongs to the organization context before reads, mutations, uploads, and upload-part operations; a real PostgreSQL regression rejects a mismatched context. This closes the same organization/archive pairing class outside webhook ingestion.

HARDENING-35 evidence: privacy-request and billing routes now revalidate authoritative archive membership even though their archive scope is carried in the body/session rather than the URL; unit coverage proves stale memberships receive `AUTH_REQUIRED` before service mutation.

HARDENING-37/38 evidence: HTTP provider adapters and DeepSeek now have bounded per-instance circuit breakers with configurable validated failure thresholds and cooldowns; deterministic tests prove retryable failures open the circuit and subsequent calls fail fast without another provider request. Provider live-fire and vendor delivery remain externally scoped.
HARDENING-39 evidence: worker fixity and derivative idempotency lookups now include organization and archive predicates before reusing existing rows, matching the database composite-tenant boundary; format, lint, typecheck, unit (79), integration (31), E2E (11), build, security, secret scan, dependency audit, reality, smoke, performance (p95 0.54ms), live-fire (16 proofs), and encrypted backup/restore (schema_migrations=11) all pass locally. Hosted worker execution and production storage/provider verification remain deferred.
HARDENING-40 evidence: migration `0012_server_side_sessions.sql` adds a PostgreSQL session inventory with hashed device metadata, expiry, and revocation state. The API exposes explicit registration, inventory, atomic same-principal rotation, self/admin revocation, and revoke-all routes; registered-row revocations are checked at the request hook without invalidating unregistered legacy bearer tokens. Real PostgreSQL tests cover metadata hashing, predecessor fencing, principal binding, inventory, and administrative revocation; focused API route tests pass. Native Better Auth/passkey/Argon2id issuance and identity-provider migration remain external/blueprint release gates.
HARDENING-41 evidence: restricted-field archive writes now emit version 2 envelopes with archive-scoped HMAC-derived wrapping keys, while version 1/global envelopes remain readable for migration. Wrong-scope decryption fails closed. Full local gates pass: unit (81), integration (34), E2E (11), build/security, secret scan, dependency audit, reality, smoke, performance (p95 1.08ms), live-fire (16 proofs), and encrypted backup/restore (schema_migrations=12). Production KMS wrapping, key rotation, escrow, and restore evidence remain deferred.
HARDENING-42 evidence: confirmed-fact mutations now require `confirmerId` to equal the authenticated actor before evidence-linked persistence; a real PostgreSQL regression proves mismatched confirmation is rejected with no row written and matching confirmation succeeds. Full local gates pass after the change: unit (81), integration (35), E2E (11), build/security, secret scan, dependency audit, reality, smoke, performance (p95 0.56ms), live-fire (16 proofs), and encrypted backup/restore (`family-historian-20260808T134608Z.dump.enc`, `schema_migrations=12`). Delegated confirmation remains an explicit future authorization workflow.
HARDENING-43 evidence: worker media downloads now enforce the authoritative original byte-size ceiling and map an over-limit stream to a non-retryable `MEDIA_INPUT_TOO_LARGE` failure; local HTTP S3-compatible endpoints use path-style addressing so Docker service names resolve correctly while production HTTPS retains virtual-host mode. The clean worker image rebuilt successfully after serializing workspace builds, the image contains `ffmpeg`, `ffprobe`, `exiftool`, `magick`, `clamscan`, `ocrmypdf`, and `python3`, and the real internal-network MinIO regression passed 3/3 tests including early termination at the byte ceiling. Host-published MinIO ports remain unavailable under the current internal-only Docker network behavior, so the full host integration suite was not rerun; no production network boundary was weakened.
HARDENING-44 evidence: Docker-internal local dependency probes now use PostgreSQL, Redis, and MinIO service DNS with stdin detached, preventing Compose exec from draining the preflight table and falsely reporting `preflight: ok` after one row. `sh scripts/preflight.sh` now evaluates every row and correctly reports 14 unresolved external/legal requirements; local database, Redis, R2, and service checks pass. The full real integration suite passes 12 files/36 tests in a disposable runner with read-only migration/environment mounts and internal service endpoints; E2E passes 3 files/11 tests; internal archive-membership and multipart live-fire proofs pass. The local connectivity test accepts `LOCAL_TELEMETRY_HEALTH_URL` for the disposable runner while retaining the loopback default. No external requirement was marked satisfied by this change.
HARDENING-45 evidence: `scripts/live-fire.sh` now dispatches only `archive-membership` and `multipart-media-ingestion` to a disposable `family-historian-worker:local` runner on `family_historian_internal` when local services and the image are available; all other proofs retain the host runner. The full wrapper now emits `live-fire: ok` with all 16 proof sentinels, including authenticated DeepSeek cache telemetry and real MinIO multipart behavior. The fallback remains unchanged when the local runner prerequisites are absent, and no production worker network configuration changed.
HARDENING-46 evidence: the live-fire internal runner now discovers the active Compose network through `com.docker.compose.network=family_historian_internal` rather than a hard-coded project prefix. The label-discovered runner was exercised against the current real services and all 16 live-fire proofs passed; no external requirement or production network configuration changed.

HARDENING-47 evidence: the SQL outbox dispatcher now reclaims `running` jobs whose lease has expired, assigning a fresh UUID lock token and incrementing the attempt count while preserving `available_at` and token-fenced completion/failure updates. A real PostgreSQL integration regression proves a reclaimed job completes under a new token; the full disposable integration suite passes 12 files/37 tests, E2E passes 3 files/11 tests, and all local quality, security, secret, dependency, reality, smoke, performance, and sixteen live-fire gates pass. No external requirement changed; production queue topology and hosted lease behavior remain deferred.

HARDENING-48 evidence: source review identified an evidence-empty publication readiness acceptance path, same-archive upload-session takeover without initiator ownership checks, and media side effects not fenced to the active outbox lease. The report schema now requires non-empty rights/consent/citation arrays; upload routes revalidate live membership and service methods require `initiated_by_user_id`; media quarantine/fixity/derivative writes verify and lock the active outbox token and stale lease errors no longer mark newer scans failed. Unit (24 files/82 tests), disposable real-PostgreSQL integration (12 files/38 tests), E2E (3 files/11 tests), build, security, secret, dependency, reality, smoke, performance (p95 1.61ms), and all 16 live-fire proofs pass. The fresh Codex Security standard scan `7d65e2cc-512c-42bf-b07c-4db13024415f` targets pre-fix revision `692176d`; it remains running at discovery 0/322 with no findings conclusion inferred. Hosted queue heartbeats/sandbox evidence and legal/external requirements remain deferred.
HARDENING-49 evidence: narration worker intake now joins the tenant-scoped `voice_authorizations` row and rejects `revoked_at` authorizations with a non-retryable `PERMISSION_DENIED` before moving the narration job to running/review-required. The disposable real-PostgreSQL privacy-worker suite passes 3/3, including the revoked-authorization regression; typecheck, lint, and format pass. Downstream narration synthesis and publication fulfillment remain deferred implementation families, so no provider output or live narration effect was claimed.
HARDENING-50 evidence: the publication bundle now requires the authoritative current edition hash as a separate input and rejects invalid or mismatched readiness hashes before rendering. The focused publishing suite passes 3/3 and the full unit suite passes 24 files/83 tests; typecheck, lint, and format pass. Domain publication approval and external accessibility/live provider evidence remain separate gates.
HARDENING-51 evidence: the worker media plan previously passed unsupported `--fdpass` to the installed `clamscan`; the plan now uses the supported `--no-summary` argv and the media unit suite passes 8/8. Typecheck, lint, and format pass. A direct worker-image probe confirms the corrected flag reaches ClamAV, which then fails closed because `/var/lib/clamav` has no supported signature database; representative media fixture execution and signature-database provisioning remain release work. Aggregate verification also recorded a Resend 400 credential probe failure, increasing unresolved preflight requirements to 15; no provider success is inferred.
HARDENING-52 evidence: the local worker profile now provisions real ClamAV signatures through a healthy official updater service with egress isolated to that service; the worker mounts signatures read-only on the internal-only network. The media plan writes ffprobe metadata to a declared scratch file, and metadata outputs are no longer persisted as derivatives. A real internal-network fixture passed 1/1 with ClamAV, ffprobe, FFmpeg, PostgreSQL, MinIO, fixity, quarantine, derivative, and outbox assertions. Host build/typecheck/lint/unit/security/compose gates pass; a plain-progress Docker rebuild completed and the fresh worker image passed the media-tool probe and the same real fixture.
HARDENING-53 evidence: AI gateway cache hits now validate the complete cached envelope, including provenance strings, non-negative usage counters, and bounded cache ratio, before output-schema parsing and reuse. A malformed telemetry envelope is deleted and recomputed through the provider; the focused gateway suite passes 5/5 and the full unit suite passes 24 files/84 tests. Build, security, typecheck, lint, and format pass. Hosted cache backend and retention/location evidence remain deferred.
HARDENING-54 evidence: production archive routes now revalidate current database permissions after signed-claim validation; owner roles retain explicit archive-wide authority while other roles require a current `permission_grants` row. A stale-permission unit regression passes 7/7 and route execution is skipped after the checker denies access. The media scan catch path also verifies the active outbox lease before marking quarantine `error`, preventing a reclaimed attempt from corrupting a newer scan. Typecheck, lint, and format pass; the disposable E2E database suite could not run because PostgreSQL host port `35432` was unavailable, so no external or integration success is inferred.
HARDENING-55 evidence: PostgreSQL-backed session inventory and revoke-all operations now include the authenticated organization in their predicates, and targeted revoke rejects a stored session from another organization even when the user ID matches. Unit coverage remains 24 files/85 tests, and the real internal PostgreSQL session-store suite now passes 3/3 with same-user sessions in two organizations isolated during listing and revoke-all. The repository host-port integration wrapper remains unavailable, but the required database proof was completed through the internal runner.
HARDENING-56 evidence: the dispatcher now renews active outbox leases at one-third of their configured duration with serialized updates and a 250 ms floor, preserving margin for media tools whose bounded execution budget exceeds the default five-minute lease. The first real heartbeat regression exposed an insufficient one-second clamp and was corrected; the rerun passed all 7 PostgreSQL dispatcher tests, including proof that a second worker cannot reclaim a sleeping first handler. Hosted queue topology and failure-injection evidence remain deployment gates.
HARDENING-57 evidence: handoff and production-readiness records now distinguish historical checkpoint evidence from current status; current blocker count is 15 and real internal media, dispatcher heartbeat, and session isolation proofs are listed as passed. Historical checkpoint narratives remain preserved.
HARDENING-58 evidence: request-time membership and permission checker exceptions now return a redacted retryable `PROVIDER_UNAVAILABLE` 503 instead of a generic 500, while false checker results remain fail-closed 401/403 denials. Focused API authorization coverage passes 9/9, including request-time membership lookup outage, and the full unit suite passes 24 files/87 tests; typecheck and formatting pass after the regression addition. Build, security, and secret-scan evidence remains from the same source checkpoint. Hosted authorization-provider availability remains an external gate.
HARDENING-59 evidence: signed session principals now cap archive memberships at 256, permissions at 256 entries with 128-character values, and bearer payloads at bounded sizes before parsing. Focused auth coverage passes 9/9; full unit coverage passes 24 files/88 tests; build, security, typecheck, lint, format, and secret-scan gates pass. No external requirement changed.
HARDENING-60 evidence: portable JSONL serialization now canonicalizes nested object keys, and portable manifests parse every JSONL entry and reject mismatched entry counts before recording fixity. Focused export coverage passes 4/4; full unit coverage passes 24 files/89 tests; build, security, typecheck, lint, format, and secret-scan gates pass. No external requirement changed.
HARDENING-61 evidence: confirmed-fact contract evidence-link arrays now cap at 1,000 before tenant-scoped SQL fan-out and per-link persistence. Focused contract coverage passes 3/3; full unit coverage passes 24 files/90 tests; build, security, typecheck, lint, format, and secret-scan gates pass. No external requirement changed.
HARDENING-62 evidence: resumable-export completion now rejects duplicate or out-of-manifest provider part numbers before missing-part evaluation. Focused export coverage passes 3/3; full unit coverage passes 24 files/91 tests; build, security, typecheck, lint, format, and secret-scan gates pass. No external requirement changed.
HARDENING-63 evidence: resumable-export planning now validates generatedAt as an RFC 3339 timestamp before hashing or returning a manifest. Focused export coverage remains 3/3; full unit coverage passes 24 files/91 tests; build, security, typecheck, lint, format, and secret-scan gates pass. No external requirement changed.
HARDENING-64 evidence: deletion workflow request, grace-period, and transition timestamps now fail closed when non-finite, and evidence requires an allowed target, non-empty reference, and parseable verifiedAt timestamp. Focused deletion invariants pass 11/11; full unit coverage passes 24 files/92 tests; build, security, typecheck, lint, format, and secret-scan gates pass. Deletion execution, storage-provider authentication, and production retention/legal approvals remain deferred.
HARDENING-65 evidence: media scan completion now locks the tenant-scoped original row and compare-and-sets `scanning` to `clean`; an infected or unexpected concurrent state cannot be overwritten by a stale or racing handler. Media unit coverage (8/8), full unit coverage (24 files/92 tests), build, security, typecheck, lint, format, and secret-scan gates pass. The direct PostgreSQL/MinIO media-worker integration command was attempted but failed with `ECONNREFUSED 127.0.0.1:35432`; no integration success is inferred. Hosted worker lease and sandbox evidence remain deferred.
HARDENING-66 evidence: billing quota checks now reject invalid current or grace-period timestamps before evaluating past-due access. Focused billing coverage passes 3/3; full unit coverage passes 24 files/93 tests; build, security, typecheck, lint, format, and secret-scan gates pass. Provider billing live-fire and production account evidence remain deferred.
HARDENING-67 evidence: Stripe signature verification now rejects invalid or unsafe clock and tolerance settings before freshness checks. Focused provider coverage passes 4/4; full unit coverage passes 24 files/93 tests; build, security, typecheck, lint, format, and secret-scan gates pass. Authenticated Stripe delivery and production webhook endpoint evidence remain deferred.
HARDENING-68 evidence: the AI gateway now rejects invalid, non-positive, or overlarge maxInputTokens values before policy/provider dispatch. Focused gateway coverage passes 6/6; full unit coverage passes 24 files/94 tests; build, security, typecheck, lint, format, and secret-scan gates pass. Authenticated AI-provider live-fire and production data-processing approvals remain deferred.
HARDENING-68 verification recheck: `sh scripts/live-fire.sh` passed all 16 proof sentinels using the internal runner for archive-membership and multipart proofs. `sh scripts/test-integration.sh`, `sh scripts/verify.sh`, and `sh scripts/production-readiness-check.sh` were attempted; each remained blocked before integration by the fail-closed preflight or host-only service ports, with no success inferred.
HARDENING-76/77 evidence: the prescribed integration and E2E wrappers now use a disposable worker-image runner on the labeled internal Compose network when host ports are unavailable. Complete integration passes 13 files/41 tests, E2E passes 3 files/11 tests, and the media fixture uses real ClamAV signatures, FFmpeg, PostgreSQL, and MinIO. The OTLP probe similarly checks the real internal collector when host forwarding is unavailable. Hosted provider telemetry and production delivery remain deferred; no external credential was marked satisfied.
HARDENING-126 evidence: session metadata is now bounded before hashing or persistence (device label 256 chars, user-agent 2,048 chars, IP address 128 chars) for both registration and rotation. Unit auth coverage (12/12), disposable real-service integration (13 files/41 tests), E2E (3 files/11 tests), accessibility, build, dependency, reality, smoke, live-fire, and performance (p95 0.55ms) passed. This changes no external requirement; identity-provider migration, hosted session infrastructure, and production credential/evidence gates remain deferred.
HARDENING-127 evidence: direct streamed object downloads now default to and cannot exceed the 25 GiB `MAX_STREAMED_OBJECT_BYTES` ceiling. The real MinIO storage suite and complete disposable integration suite pass (13 files/41 tests), with unit 27 files/130 tests, E2E 3 files/11 tests, accessibility, build, dependency, reality, smoke, live-fire, and performance (p95 0.56ms) also green. Cloud-provider authenticated probes and hosted storage lifecycle evidence remain deferred.
HARDENING-128 evidence: multipart presigned part URLs now reject non-integer, non-positive, and over-one-hour expiry values before signing; the 900-second API default remains. Real MinIO integration and the complete local matrix pass (unit 27 files/130 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.54ms). Authenticated cloud-provider and hosted storage lifecycle evidence remain deferred.
HARDENING-129 evidence: fixed-window and Redis rate limiters now enforce safe finite configuration bounds (count <= 1,000,000, window <= seven days, in-memory keys <= 100,000) and reject invalid injected timestamps. Full local verification passes (unit 27 files/130 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.62ms). Hosted Redis capacity/failover evidence and external production observability remain deferred.
HARDENING-130 evidence: observability helpers now bound context fields, metric names/units, label count, label key/value sizes, and timestamps before materialization; focused coverage passes 6/6 and full local verification passes (unit 27 files/131 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.55ms). Hosted telemetry delivery, retention, and vendor evidence remain deferred.
HARDENING-131 evidence: annual preservation review construction now validates unknown runtime finding objects, rejects duplicate or unknown checks, requires valid statuses and non-empty detail capped at 2,048 characters, and caps the set to the seven required checks. After the bounded recovery of an initial unsafe-any/fixture failure, full local verification passes (unit 27 files/132 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.54ms). External retention/legal approval and hosted preservation evidence remain deferred.
HARDENING-132 evidence: quotation source spans now enforce offset-length fidelity (`endOffset - startOffset === text.length`) and the cited-memoir live-fire fixture derives offsets from its source text. Full local verification passes (unit 27 files/132 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.50ms). No external requirement changed; provider, hosted, and legal/approval evidence remains deferred.
HARDENING-133 evidence: publication readiness reports now bound item labels to 256 characters, reasons to 2,048 characters, and each evidence category to 256 items. Full local verification passes (unit 27 files/132 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.56ms). No external requirement changed; provider, hosted, and legal/approval evidence remains deferred.
HARDENING-134 evidence: multipart completion contracts and storage-provider validation now cap ETag and checksum tokens at 1,024 characters, including provider-returned part listings. Full local verification passes (unit 27 files/134 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.57ms). No external requirement changed; provider, hosted, and legal/approval evidence remains deferred.
HARDENING-135 evidence: the Fly worker manifest and security harness now enforce the no-public-ingress invariant, while the worker health server is verified to bind loopback only. Full local verification passes (unit 27 files/134 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.52ms). Hosted syscall, egress, cgroup/PID, read-only-root, scratch, and signed attestation evidence remain deferred.
HARDENING-136 evidence: `PRODUCTION_READINESS.md` now reports the current 27-file/134-test unit matrix and explicitly includes the private-worker ingress guard; the hosted sandbox attestation remains fail-closed and unchanged.
HARDENING-137 evidence: provider schemas now bound parsed Deepgram, Resend, Turnstile, and Stripe fields/collections in addition to aggregate response-byte ceilings. Full local verification passes (unit 27 files/135 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.99ms). No external requirement changed; provider credentials, hosted, and legal/approval evidence remain deferred.
HARDENING-138 evidence: malformed provider and DeepSeek response shapes now fail as stable non-retryable adapter errors without raw schema details or futile retries. Full local verification passes (unit 27 files/136 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.79ms). No external requirement changed; provider credentials, hosted, and legal/approval evidence remain deferred.
HARDENING-139 evidence: every ObjectStorage key-bearing method now validates a 1,024-character maximum and rejects control characters before provider calls. Full local verification passes (unit 27 files/137 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.50ms). No external requirement changed; provider credentials, hosted, and legal/approval evidence remain deferred.
HARDENING-140 evidence: direct storage methods now validate provider upload IDs, MIME content types, and canonical SHA-256 base64 checksums before provider calls. Full local verification passes (unit 27 files/138 tests, integration 13 files/41 tests, E2E 3 files/11 tests, all 16 live-fire proofs, performance p95 0.86ms). No external requirement changed; provider credentials, hosted, and legal/approval evidence remain deferred.
HARDENING-141 evidence: the shared AI gateway now rejects source text over 4 MiB before policy processing, generic provider content over 8 MiB before JSON parsing, oversized provider request IDs, and invalid usage telemetry. Focused and complete local verification remains pending until the implementation checkpoint is committed.
HARDENING-141 recheck: focused gateway coverage passed 9/9; full local verification passed unit 27 files/140 tests, integration 13 files/41 tests, E2E 3 files/11 tests, accessibility (`pdf_tagged=true epub_semantics=true`), all sixteen live-fire proofs, performance p95 0.57ms, workspace build, typecheck, lint, format, security, secret, dependency-audit, reality, and smoke gates. No external requirement changed.
HARDENING-143 evidence: Deepgram transcription rejects materialized input above 128 MiB before request-body copying or network dispatch; focused and full verification are pending until the checkpoint is committed.
HARDENING-143 recheck: provider coverage passed 10/10; full local verification passed unit 27 files/141 tests, integration 13 files/41 tests, E2E 3 files/11 tests, accessibility (`pdf_tagged=true epub_semantics=true`), all sixteen live-fire proofs, performance p95 0.91ms, workspace build, typecheck, lint, format, security, secret, dependency-audit, reality, and smoke gates. No external requirement changed.
HARDENING-144 evidence: storage object keys, multipart upload IDs, and content types now enforce UTF-8 byte ceilings before provider calls. Focused storage validation passed 5/5; no external requirement changed. Full local verification remains pending until the checkpoint is committed.
HARDENING-144 recheck: full local verification passed unit 27 files/141 tests, integration 13 files/41 tests, E2E 3 files/11 tests, accessibility (`pdf_tagged=true epub_semantics=true`), all sixteen live-fire proofs, performance p95 1.60ms, workspace build, typecheck, lint, format, security, secret, dependency-audit, reality, and smoke gates. No external requirement changed; production remains blocked by the existing 16 unresolved requirements.
HARDENING-165 evidence: the Deepgram transcription live-fire probe now bounds provider response materialization to 8 MiB and neutralizes untrusted request identifiers in logs. No external requirement changed; authenticated Deepgram evidence remains operator-supplied and production release remains blocked by the existing unresolved register.
HARDENING-166 evidence: the shared AI gateway now validates model and prompt metadata, caps the stable policy prefix, and rejects serialized dynamic input above 8 MiB before cache-key construction or provider dispatch. No external requirement changed; authenticated AI-provider live-fire and data-processing approvals remain deferred.
