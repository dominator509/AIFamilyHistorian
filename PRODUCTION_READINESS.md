# Production Readiness

Current status is **MAXIMUM_ENGINEERING_COMPLETE**, not production approved. All sixteen locally executable live-fire proofs pass against real local PostgreSQL, Redis, MinIO, Mailpit, and OTLP services, including the authenticated DeepSeek cache/provenance proof. The SQL outbox dispatcher now fences reclaimed leases, supports validated archive partitions, and enforces idempotent derivative recipes; upload and archive outbox capacity decisions are serialized inside PostgreSQL transactions; billing state is provider-authoritative with database-enforced subscription integrity and durable signature-verified Stripe callback ingestion; production CORS fails closed without an explicit HTTPS allowlist; media quarantine, privacy-request intake, export intake, and narration intake have real database integration coverage, and the API uses a Redis-backed distributed rate limiter proven against local Redis; the dedicated worker image builds with the required media executables. Lint, formatting, typecheck, unit, integration, E2E, build, security, dependency, reality, smoke, migration, and encrypted backup/restore gates pass locally, with schema_migrations=10.

`sh scripts/verify.sh` and `sh scripts/production-readiness-check.sh` remain fail-closed; aggregated `sh scripts/preflight.sh` currently reports 14 unresolved external requirements. Hosted provider probes, CI/staging/deployment verification, remaining worker job families and representative media fixtures, production KMS backup wrapping/retention and restore drills, formal accessibility/performance audits, production secret injection, and legal/vendor/insurance/DPIA/data-region approvals remain unverified. Local encrypted backup and disposable restore-check pass. No production release tag or deployment may be claimed until those gates emit their genuine sentinels.

The authoritative deferred register is `.agent/state/DEFERRED_EXTERNALS.md`; the current repository state and exact resume commands are in `REMOTE_SESSION_HANDOFF.md`.
