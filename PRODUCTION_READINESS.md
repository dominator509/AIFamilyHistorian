# Production Readiness

Current status is **MAXIMUM_ENGINEERING_COMPLETE**, not production approved. All sixteen locally executable live-fire proofs pass against real local PostgreSQL, Redis, MinIO, Mailpit, and OTLP services, including the authenticated DeepSeek cache/provenance proof. The SQL outbox dispatcher now fences reclaimed leases, supports validated archive partitions, and enforces idempotent derivative recipes; upload and archive outbox capacity decisions are serialized inside PostgreSQL transactions; multipart completion validates bounded magic-byte signatures before immutable persistence; bearer sessions support explicit current-session logout through the Redis deny-list plus opt-in PostgreSQL session registration, inventory, atomic rotation, and administrative revocation; restricted-field archive writes use versioned archive-scoped wrapping-key derivation and reject cross-archive decryption; the dedicated worker profile declares read-only, capability-free, no-new-privileges, PID, memory, CPU, and scratch-tmpfs ceilings and is attached to an internal-only local Docker network; billing state is provider-authoritative with database-enforced subscription integrity, durable signature-verified Stripe callback ingestion, authoritative archive-to-organization metadata validation, and stale-membership rejection; composite tenant foreign keys now enforce organization/archive pairs at the database boundary and worker fixity/derivative idempotency reads reassert that same organization/archive scope; DeepSeek and provider adapters now fail fast through bounded per-adapter circuit breakers after repeated retryable failures; production CORS fails closed without an explicit HTTPS allowlist; media quarantine, privacy-request intake, export intake, and narration intake have real database integration coverage, and the API uses a Redis-backed distributed rate limiter proven against local Redis; the dedicated worker image builds with the required media executables. Lint, formatting, typecheck, unit, integration, E2E, build, security, dependency, reality, smoke, migration, and encrypted backup/restore gates pass locally, with schema_migrations=12.

`sh scripts/verify.sh` and `sh scripts/production-readiness-check.sh` remain fail-closed; aggregated `sh scripts/preflight.sh` currently reports 14 unresolved external requirements. Hosted provider probes, CI/staging/deployment verification, remaining worker job families and representative media fixtures, production KMS backup wrapping/retention and restore drills, formal accessibility/performance audits, production secret injection, and legal/vendor/insurance/DPIA/data-region approvals remain unverified. The tracked-secret scan now passes locally and is part of CI verification; it does not replace provider secret rotation or production secret-manager evidence. Local encrypted backup and disposable restore-check pass. No production release tag or deployment may be claimed until those gates emit their genuine sentinels.

The authoritative deferred register is `.agent/state/DEFERRED_EXTERNALS.md`; the current repository state and exact resume commands are in `REMOTE_SESSION_HANDOFF.md`.
